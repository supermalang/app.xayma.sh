{% extends '@EasyAdmin/page/content.html.twig' %}

{% block main %}
    <iframe id="myframe" 
        src="{{ path('file_manager', {conf:'default', tree:false }) }}" 
        width="100%" height="500"
        frameborder="0">
    </iframe>
{% endblock %}